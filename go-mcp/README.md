# mock-api-service-mcp

MCP server exposing the MockApiService SDK as tools, built on the
[official Go MCP SDK](https://github.com/modelcontextprotocol/go-sdk)
and the sibling Go SDK at `../go`.

## Tools

| Tool | Args | Returns |
|------|------|---------|
| `mock-api-service_list` | `entity`, optional `query` map | First page of records as JSON |
| `mock-api-service_load` | `entity`, `query` (e.g. `{id:N}`) | Single record as JSON |

JSON schemas for both tools are emitted by the SDK from the `Args`
struct's `json` / `jsonschema` tags — no schema is hand-written.

## Entities

health | post | user

## Build

```sh
go build -o mock-api-service-mcp ./...
```

## Run

```sh
# stdio transport — for spawnable agent installs (default).
./mock-api-service-mcp

# streamable HTTP transport — share one running server between agents.
./mock-api-service-mcp -transport http -addr :8080
```

## Install for Claude Code

```sh
claude mcp add --scope user mock-api-service \
  -- /absolute/path/to/mock-api-service-mcp -transport stdio
```

After install, restart Claude Code; the `mock-api-service_list` and
`mock-api-service_load` tools become available in new sessions.

## Smoke test via HTTP

```sh
./mock-api-service-mcp -transport http -addr :18080 &

# initialize, grab the session id
curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -D headers \
  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"smoke","version":"0"}}}'

SESSION=$(awk '/Mcp-Session-Id/ {print $2}' headers | tr -d '\r')

curl -sN -X POST http://localhost:18080 \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json, text/event-stream' \
  -H "Mcp-Session-Id: $SESSION" \
  -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"mock-api-service_load","arguments":{"entity":"health","query":{"id":1}}}}'
```

## Generated by

sdkgen `go-mcp` target. See the target source under
`.sdk/src/cmp/go-mcp/` in this repo, or upstream at
`github.com/voxgig/sdkgen/project/.sdk/src/cmp/go-mcp/`.
